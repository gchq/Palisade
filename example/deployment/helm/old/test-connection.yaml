apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "palisade.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "palisade.name" . }}
    helm.sh/chart: {{ include "palisade.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "palisade.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
