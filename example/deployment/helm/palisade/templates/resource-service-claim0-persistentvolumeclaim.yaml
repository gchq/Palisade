apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: resource-service
  name: resource-service-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi