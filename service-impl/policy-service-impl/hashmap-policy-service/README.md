# HashMap Policy Service

A simple implementation of a Policy Service that simply stores the policy
rules in a ConcurrentHashMap. By default the map is static so it will
be shared across the same JVM.
